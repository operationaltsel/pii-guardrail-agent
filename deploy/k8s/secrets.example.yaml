# DO NOT commit real secrets. Create with:
#   kubectl create secret generic cs-agent-secrets -n pii-guardrail \
#     --from-literal=google-api-key=YOUR_KEY
# or manage via External Secrets Operator / GCP Secret Manager in a real deployment.
apiVersion: v1
kind: Secret
metadata:
  name: cs-agent-secrets
  namespace: pii-guardrail
type: Opaque
stringData:
  google-api-key: "REPLACE_ME"
